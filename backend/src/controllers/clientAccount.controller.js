import bcrypt from 'bcryptjs';
import Organization from '../models/Organization.js';
import User from '../models/User.js';
import { asyncHandler } from '../utils/asyncHandler.js';
import { ConflictError, NotFoundError, ValidationError } from '../utils/errors.js';
import { generateTemporaryPassword, sendTemporaryAccountEmail } from '../services/temporaryPassword.service.js';
const EMAIL_RE=/^[^\s@]+@[^\s@]+\.[^\s@]+$/; const clean=v=>String(v??'').trim(); const normalizeEmail=v=>clean(v).toLowerCase(); const makeSlug=v=>clean(v).toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-+|-+$/g,'');
function pub(o){const a=o.adminUserId;return{id:String(o._id),name:o.name,slug:o.slug,status:o.status,createdAt:o.createdAt,updatedAt:o.updatedAt,admin:a&&typeof a==='object'?{id:String(a._id),fullName:a.fullName,email:a.email,status:a.status}:null};}
export const createOrganizationWithTemporaryPassword=asyncHandler(async(req,res)=>{const companyName=clean(req.body.companyName),adminName=clean(req.body.adminName),adminEmail=normalizeEmail(req.body.adminEmail);if(!companyName||!adminName||!adminEmail)throw new ValidationError('Company name, Admin name and Admin email are required.');if(!EMAIL_RE.test(adminEmail))throw new ValidationError('Admin email is invalid.');if(await User.exists({email:adminEmail}))throw new ConflictError('A user with this Admin email already exists.');let slug=makeSlug(companyName)||'client',baseSlug=slug,counter=2;while(await Organization.exists({slug}))slug=`${baseSlug}-${counter++}`;const o=await Organization.create({name:companyName,slug,status:'active',createdBySuperAdminId:req.currentSuperAdmin._id});const temporaryPassword=generateTemporaryPassword();try{const shortId=String(o._id).slice(-8);const u=await User.create({fullName:adminName,email:adminEmail,nationalId:`SAAS-ADMIN-${shortId}`,passwordHash:await bcrypt.hash(temporaryPassword,12),passwordChangedFromDefault:false,mustChangePassword:true,role:'admin',organizationId:o._id,employeeId:`ADMIN-${shortId.toUpperCase()}`,cnic:'',designation:'Administrator',department:'Administration',gradeId:null,managerId:null,canApproveOtherDepartments:true,dateOfJoining:new Date(),detailsStatus:'complete',pendingFields:[],status:'active'});o.adminUserId=u._id;await o.save();const p=await Organization.findById(o._id).populate('adminUserId','fullName email status');const emailSent=await sendTemporaryAccountEmail({to:adminEmail,fullName:adminName,roleLabel:'Client Admin',companyName,temporaryPassword});if(!emailSent){await User.deleteOne({_id:u._id});await Organization.findByIdAndDelete(o._id);throw new ValidationError('Client Admin email could not be sent, so the new client account was not kept. Please check SMTP and try again.');}return res.status(201).json({success:true,data:pub(p),credentials:{email:adminEmail,password:temporaryPassword,temporaryPassword:true,emailSent},emailSent});}catch(e){await Organization.findByIdAndDelete(o._id);throw e;}});
export const resetClientAdminToTemporaryPassword=asyncHandler(async(req,res)=>{const o=await Organization.findById(req.params.id);if(!o||!o.adminUserId)throw new NotFoundError('Client Admin was not found.');const a=await User.findById(o.adminUserId);if(!a)throw new NotFoundError('Client Admin was not found.');const temporaryPassword=generateTemporaryPassword(),prev={hash:a.passwordHash,changed:a.passwordChangedFromDefault,must:a.mustChangePassword,refresh:a.refreshTokenHash};a.passwordHash=await bcrypt.hash(temporaryPassword,12);a.passwordChangedFromDefault=false;a.mustChangePassword=true;a.refreshTokenHash=null;a.failedLoginAttempts=0;a.lockedUntil=null;await a.save();const emailSent=await sendTemporaryAccountEmail({to:a.email,fullName:a.fullName,roleLabel:'Client Admin',companyName:o.name,temporaryPassword});if(!emailSent){a.passwordHash=prev.hash;a.passwordChangedFromDefault=prev.changed;a.mustChangePassword=prev.must;a.refreshTokenHash=prev.refresh;await a.save();throw new ValidationError('Temporary password email could not be sent. The Client Admin password was left unchanged.');}return res.json({success:true,message:'A new temporary password was generated. Client Admin must change it after login.',credentials:{email:a.email,password:temporaryPassword,temporaryPassword:true,emailSent},emailSent});});
